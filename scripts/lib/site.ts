// Kept in sync with astro.config.mjs's `site` value — used to build
// absolute links for the notification emails, which are read outside the
// site itself (in an inbox) so relative links won't work.
export const SITE_URL = 'https://gururu.griffinbearden.workers.dev';
